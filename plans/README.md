# Implementation Plans

Generated by the improve skill on 2026-07-11 against commit `990e09f`
(note: the working tree carried uncommitted PostHog-wizard edits at audit
time — plan 001 targets exactly those). Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

**Non-interactive default**: this run had no user available at the selection
gate, so per the skill's default the top findings by leverage (3 of 6 vetted)
were planned automatically. The remaining findings are recorded below for a
future session to promote or reject.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Repair PostHog wizard breakage (curly-quote JS syntax errors + dead token wiring) | P1 | S | — | DONE (PR #115, 2026-09-10) |
| 002  | CI syntax gate for inline scripts in built HTML | P1 | S | — | TODO |
| 003  | RSS feed for the Insights hub | P3 | S | — (land after 001) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 and 002 are independent, but 002's verification doubles as proof of 001:
  run 002's check against the pre-001 tree and it fails on `index.html` /
  `discovery.html`; after 001 it passes.
- 003 should land after 001 only because 001 owns the current dirty diff on
  `insights.astro` / `BaseLayout.astro`; there is no code dependency.

## Findings audited but not planned (this run)

- **No Content-Security-Policy header** (`site/public/_headers:1-5`; site embeds
  PostHog, Cal.com, Web3Forms) — real hardening, but MED risk of breaking the
  Cal.com/PostHog embeds and needs manual browser testing of every third-party
  surface. Deferred, not rejected; promote when someone can smoke-test embeds.
- **Untracked wizard artifacts** (`posthog-self-driving-report.md`,
  `site/posthog-setup-report.md`, `site/.claude/`) — disposition is a
  maintainer judgment call (keep/relocate/delete), not an executor task.
  Noted in plan 001's maintenance notes.

## Findings considered and rejected

- **Consolidate the two standalone insights essays into the `articles`
  collection**: explicitly decided against in `site/src/pages/insights.astro:8-9`
  ("ponytail: ... Promote to the collection only if these multiply"). Settled
  tradeoff, not debt.
- **Auto-generate the llms.txt Pages section**: the PR workflow already fails
  when a built page isn't referenced in llms.txt; descriptions are editorial
  copy, not derived data. Regeneration would buy little.
- **`sitemap.xml` lastmod = build date for every URL**: mild SEO noise, harmless
  in practice for a 12-URL site.
- **Web3Forms shared free-tier quota / spam on the leakable /discovery URL**:
  already tracked as GitHub issue #46 (Turnstile follow-up) and documented in
  `CLAUDE.md`; not re-planned here.
- **No parity baseline for `insights.md`**: the Insights index changes with
  every published article; a byte-parity baseline would churn on each post.
  The fixed-page baselines cover the pages whose copy is actually locked.
- **`services_page_viewed` / `checklist_page_viewed` duplicate PostHog's
  automatic `$pageview`**: redundant but cheap; dashboards were built on the
  named events. Not worth unwinding.
